#!/usr/bin/env bash
# AWS EC2 image tasks

# Load environment
# shellcheck disable=1090
. "$(cd "$(dirname "${BASH_SOURCE[0]}")/../" && pwd -P)/envrc"

# Process arguments
case "${1:-}" in
  create)
    ami_create
    ;;
  clean)
    ami_clean
    ;;
  *)
    e_abort "USAGE: ${BASH_SOURCE[0]} [create | clean]"
    ;;
esac
