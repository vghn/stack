#!/usr/bin/env bash
# Release tasks

# Load environment
# shellcheck disable=1090
. "$(cd "$(dirname "${BASH_SOURCE[0]}")/../" && pwd -P)/envrc"

vgs_release \
  --type "${1:-patch}" \
  --github-url 'https://github.com/vghn/puppet_stk'
